@page "/cascadingvaluesparameterstheme"
@*@layout CascadingValuesParametersLayout*@
@using BlazorApp2.Client.UIThemeClasses

<h1>Cascading Values & Parameters</h1>

<p>Current count: @currentCount</p>

<p>
    <button class="btn" @onclick="IncrementCount">
        Increment Counter (Unthemed)
    </button>
</p>

<p>
    <button class="btn @ThemeInfo.ButtonClass" @onclick="IncrementCount">
        Increment Counter (Themed)
    </button>
</p>
<p>
    The main layout razor has a cascading parameter.

    This can be referred to in any child component without having to pass it in as a parameter manually.

    It is a class with a single string value called Button Class
</p>
@code {
    private int currentCount = 0;

    [CascadingParameter]
    protected ThemeInfo ThemeInfo { get; set; }

    private void IncrementCount()
    {
        currentCount++;
        ThemeInfo.ButtonClass = "btn-success";

    }
}
